import { TimezoneFormatterPipe } from './timezone-formatter.pipe';

describe('TimezoneFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimezoneFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
